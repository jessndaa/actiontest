name: Check all node 

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  echo_github_gestristed:
    name: echo git hub
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo's default branch
        uses: actions/checkout@v3
      - name: actions/setup-node@v2
        with:
          node-version: 14.x
      - name: test sum application
        run: npm run test
      - name: echo secret
        run: echo ${{secrets.TOKEN}}
